Cậu ơi, tớ đọc bài này.

Applying the MVC concept https://www.mql5.com/en/articles/17653

Họ viết về cái function này.

string TypeDescription(const ENUM_OBJECT_TYPE type)

string TypeDescription(const ENUM_OBJECT_TYPE type)

  {

   string array[];

   int total=StringSplit(EnumToString(type),StringGetCharacter("_",0),array);

   string result="";

   for(int i=2;i<total;i++)

     {

      array[i]+=" ";

      array[i].Lower();

      array[i].SetChar(0,ushort(array[i].GetChar(0)-0x20));

      result+=array[i];

     }

   result.TrimLeft();

   result.TrimRight();

   return result;

  }

Tớ hơi confuse một tí.

Sao đầu vào của function const ENUM_OBJECT_TYPE type mà trong đó lại có string array[];

Và result+=array[i];

rồi return result;

Cậu nhỉ.